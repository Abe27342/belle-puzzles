import { describe, expect, it } from 'vitest';

describe('App', () => {
	// TODO: Make top-level smoke tests work.
	// https://testing-library.com/docs/example-react-router/
	it('dummy test', () => {
		expect(1 + 1).toEqual(2);
	});
});
